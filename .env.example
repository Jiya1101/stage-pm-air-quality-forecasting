# Copy this file to .env and fill in the keys you have. .env is git-ignored —
# it will never be committed. Scripts load it automatically via _pathfix.py.

OPENAQ_API_KEY=
CPCB_API_KEY=
FIRMS_MAP_KEY=
# ERA5 does not use an env var -- its key lives in ~/.cdsapirc (see src/aqf/data/era5.py)
